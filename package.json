{
  "name": "super-smash-mobs-brawl",
  "private": true,
  "scripts": {
    "start": "dotenv -- turbo start",
    "build:api": "turbo build --filter=env --filter=api --filter=wrangler --filter=tussler --force",
    "start:api": "dotenv -- turbo start --filter=api",
    "dev:api": "dotenv -- turbo dev --filter=api",
    "dev:orchestrator": "dotenv -- turbo dev --filter=orchestrator",
    "build:plugin": "",
    "db:push": "dotenv -- turbo db:push --filter=api",
    "db:studio": "dotenv -- turbo db:studio --filter=api",
    "build": "turbo build",
    "dev": "turbo dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "dotenv-cli": "^7.3.0",
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=20"
  }
}
