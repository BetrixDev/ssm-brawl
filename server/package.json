{
  "name": "server",
  "scripts": {
    "build": "go build -o build/start-server.exe -ldflags \"-s -w\"",
    "start": "exec-bin ./build/start-server.exe",
    "dev": "go run main.go"
  },
  "dependencies": {
    "exec-bin": "^1.0.0"
  }
}
